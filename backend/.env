# Django
DJANGO_SECRET_KEY=troque-esta-chave-em-producao-use-algo-longo-e-aleatorio
DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1

# Banco de dados (PostgreSQL)
DB_NAME=gestao_juridica
DB_USER=postgres
DB_PASSWORD=c4rl0s4554
DB_HOST=localhost
DB_PORT=5432

# Frontend (TanStack Start) — origem permitida para CORS
CORS_ALLOWED_ORIGINS=http://localhost:3000

# Cookies de autenticação
# Em produção (HTTPS): AUTH_COOKIE_SECURE=True e AUTH_COOKIE_SAMESITE=None
AUTH_COOKIE_SECURE=False
AUTH_COOKIE_SAMESITE=Lax
